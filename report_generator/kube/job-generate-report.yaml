apiVersion: batch/v1
kind: Job
metadata:
  name: archivum-report-generator
spec:
  completions: 1
  backoffLimit: 4
  template:
    metadata:
      labels:
        app: archivum-report-generator
    spec:
      containers:
        - name: archivum-report-generator
          image: homelab.local:30005/archivum-report-generator:latest
          imagePullPolicy: Always
          env:
            - name: PASTEBIN_API_KEY
              valueFrom:
                secretKeyRef:
                  name: archivum
                  key: pastebinApiKey
            - name: CONFIG_PATH
              value: "/app/config_files/deploy_config.yml"
            - name: DATABASE_HOST_ADDRESS
              value: "mysql"
          args:
            - "-n"
            - "fallenshadow"
      restartPolicy: Never

