#!/usr/bin/env zsh

kubectl apply -f "$PWD/report_generator/kube/job-generate-report.yaml"

sleep 3
kubectl wait --for=condition=Ready pod -l job-name="archivum-report-generator" 

kubectl logs -f job/archivum-report-generator

echo "\n\n"

kubectl delete job "archivum-report-generator";
